1.
Styling CardHeader, CardTitle phải theo mẫu sau, giống cả className và format
<CardHeader>
                                <CardTitle className="flex items-center gap-2">
                                    <div className="p-2 rounded-full bg-primary/10">
                                        <Icon path={mdiCash} size={0.8} className="text-primary" />
                                    </div>
                                    <span>Tổng quan đơn hàng</span>
                                </CardTitle>
                            </CardHeader>
Chỉ được phép thay đổi Icon và title-text. Cấm thêm và thay đổi bất kì thứ gì khác
        2. Button

 <Button
                        variant="outline"
                        onClick={() => onOpenChange(false)}
                    >
                        <Icon path={mdiClose} size={0.8} />
                        Đóng
                    </Button>

Luật UI:
1. Không được thêm className vào <Button> trừ "flex-1" hoặc "w-full"
2. Icon sử dụng luôn là size={0.8}
3. Không sử dụng text-[10px], bé nhất phải là text-xs